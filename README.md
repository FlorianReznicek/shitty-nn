# cli

- use `yarn play-random` to play human vs a random machine configuration
- use `yarn play-version my-nn-config.json` to play human vs a specific machine configuration stored in a specified .json file
- use `yarn evolve 10000` to let the computer generate 10000 generations of self-playing ANNs